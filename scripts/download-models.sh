#!/bin/bash
# Script to download local models if using local transformers
echo "Demo mode uses heuristic scoring. No large models needed for basic demo."
echo "To use Hugging Face, set USE_HF=true in backend/.env"
